{
  "category": "dlq-spike",
  "expected_team": "queue-consumer",
  "expected_severity": "critical",
  "component": "queue-consumer",
  "group": "queues",
  "source_prefix": "queue-consumer-prod",
  "summary_template": "queue-consumer dead-lettering a large batch of messages (dead_lettered_count={dead_lettered_count}, window_minutes={window_minutes})",
  "fields": [
    { "name": "dead_lettered_count", "type": "int", "min": 105, "max": 600 },
    { "name": "window_minutes", "type": "int", "min": 5, "max": 20 }
  ]
}
